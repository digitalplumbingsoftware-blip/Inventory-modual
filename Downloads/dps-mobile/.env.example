# DPS Mobile — copy to .env and set your Mac's local IP
# For simulator: http://localhost:4000
# For real device: http://192.168.X.X:4000  (run `ipconfig getifaddr en0` to find it)
EXPO_PUBLIC_API_URL=http://YOUR_MAC_IP:4000
